<% notifications = @notifications.where.not(visiter_id: current_user.id) %>
<%= link_to "全削除", notification_path, method: :delete %>
<% if notifications.exists? %>
  <%= render "notifications", notifications: @notifications %>
<% else %>
  通知はありません
<% end %>